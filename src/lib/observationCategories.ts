import type { Category } from "@/types/observation"

/** Horse log flow (mobile sheet + horse-only UIs). */
export const HORSE_OBSERVATION_CATEGORIES: Category[] = ["Health", "Behavior"]

/** Shared log modal (horses + cattle). */
export const ALL_OBSERVATION_CATEGORIES: Category[] = ["Health", "Behavior", "Feeding", "Calving"]
