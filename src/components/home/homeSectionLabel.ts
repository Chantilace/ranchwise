/** Shared size/weight/case/tracking for home dashboard card section titles. */
const HOME_SECTION_EYEBROW_BASE = "text-xs font-medium uppercase tracking-wide"

/** Neutral cards (Season, Needs attention, branding / turnout prep). */
export const HOME_SECTION_EYEBROW = `${HOME_SECTION_EYEBROW_BASE} text-muted-foreground`

/** AI insights card — same typography as neutral eyebrows, accent color. */
export const HOME_SECTION_EYEBROW_AI = `${HOME_SECTION_EYEBROW_BASE} text-ai-accent-text`
